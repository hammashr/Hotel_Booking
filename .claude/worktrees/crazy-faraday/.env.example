# ─────────────────────────────────────────────────────────────────────────────
# The Tiny Escape — Environment Variables
# Copy this file to .env and fill in your values.
# ─────────────────────────────────────────────────────────────────────────────

# ── API / Backend ──────────────────────────────────────────────────────────────
# Point this to your backend proxy that wraps the Hostfully PMP API.
# Example (local dev):  http://localhost:5000
# Example (production): https://api.tinyescape.com
VITE_API_BASE_URL=http://localhost:5000
VITE_API_URL=http://localhost:5000

# ── Hostfully PMP ─────────────────────────────────────────────────────────────
# Your backend proxy should forward requests to:
#   https://api.hostfully.com/v2
# and attach the X-HOSTFULLY-AGENCYUID + X-HOSTFULLY-APIKEY headers.
# These keys should NEVER be placed here (frontend) — server-side only.
#
# Expected backend routes (consumed by this frontend):
#   GET  /api/houses                            → list all properties
#   GET  /api/houses/:slug                      → single property
#   GET  /api/houses/:slug/packages             → rate packages
#   GET  /api/houses/:slug/unavailable-dates    → blocked/booked dates
#   POST /api/bookings/check-availability       → { houseSlug, checkIn, checkOut }
#   POST /api/bookings                          → create booking request
#   POST /api/bookings/price-preview            → price estimate

# ── Site ──────────────────────────────────────────────────────────────────────
VITE_SITE_URL=https://tinyescape.vercel.app

# ── Feature Flags ─────────────────────────────────────────────────────────────
VITE_ENABLE_BOOKING=false
VITE_ENABLE_PAYMENTS=false

# ── Square Payments ───────────────────────────────────────────────────────────
# Square Application ID (safe to expose in frontend — starts with sq0idp-)
VITE_SQUARE_APP_ID=
# Square Location ID (safe to expose in frontend)
VITE_SQUARE_LOCATION_ID=
# Square environment: sandbox | production
VITE_SQUARE_ENV=sandbox

# ── Analytics ─────────────────────────────────────────────────────────────────
# Google Analytics 4 Measurement ID (format: G-XXXXXXXXXX)
VITE_GA_ID=
# Google Tag Manager Container ID (format: GTM-XXXXXXX) — optional if using GA4 directly
VITE_GTM_ID=
# Meta / Facebook Pixel ID
VITE_FB_PIXEL_ID=

# ── Google Maps ───────────────────────────────────────────────────────────────
VITE_GOOGLE_MAPS_KEY=

# ── CDN ───────────────────────────────────────────────────────────────────────
VITE_CDN_URL=
